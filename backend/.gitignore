# 编译产物（Nest CLI 输出目录）
/dist

# TypeScript 增量构建信息（`incremental: true`）
*.tsbuildinfo

# 依赖
/node_modules

# 本地环境变量（勿提交密钥）
.env
.env.local
.env.*.local

# 日志与调试
npm-debug.log*
*.log

# 测试覆盖率
/coverage

# 操作系统
.DS_Store

# SQLite 数据文件（本地/开发；若需入库可去掉此行）
/data/*.sqlite
/data/*.db
