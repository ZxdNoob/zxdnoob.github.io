# Git 忽略规则（根目录）：Next 与 monorepo 后端路径一并列出，避免误提交依赖与构建产物。
# 说明：https://help.github.com/articles/ignoring-files/

# backend (NestJS)：依赖、构建产物与覆盖率（与 `backend/.gitignore` 互补）
backend/node_modules/
backend/dist/
backend/coverage/
backend/*.tsbuildinfo

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
# 由 Next 生成的类型引用文件（若需在仓库中固定版本可改为不忽略）
next-env.d.ts
